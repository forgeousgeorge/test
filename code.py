
import numpy as np

def calc_circ_area(radius):
    A = np.pi*radius**2
    print A # this will make it much easier in future problems to see that something is actually happening

def calc_square_area(length, width):
    A = length*width
    print A
